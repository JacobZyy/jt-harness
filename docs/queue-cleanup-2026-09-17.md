# 队列检查与异常清理

> 历史验证：本文记录一次已完成的历史清理，不是日常维护指令；当前任务不据此删除或重跑任何队列。现行操作见[文档导航](README.md)。

2026-09-17 检查：20 个失败任务（均未保存正文），2 个会话采集游标因未支持 `FunctionCallOutput` 而报错。本地待接收事件和手动候选均为 0。

已删除 jt-harness 项目范围内这 20 个失败队列任务，保留成功任务、记忆、向量与原始来源。删除任务 ID 记录于忽略的 `artifacts/jth/queue-cleanup/deleted-jobs.json`。采集游标及已处理消息集合未重置，因此不会自动重新投递被清理的失败批次。

`FunctionCallOutput` 按工具证据解析，保存 name、namespace、output。两个会话的待处理原文区间经新解析器完整读取通过后，清除了旧错误以及指向已删除任务的 last_receipt；未跳过正常消息，未推进游标，也未重放或重新生成。

验证：构建、类型检查通过；7 项采集回归通过，新增用例确认 FunctionCallOutput 不阻塞后续用户消息。检查时失败、排队、运行任务均为 0；15 个 DSH 完成任务、2 个手动索引完成任务保留。数据库体检 0 错误，唯一历史警告为旧记忆缺少来源时间。

提炼 Agent 已要求没有值得记忆的内容时返回三个空数组。该情况下 DSH 提取已经执行，但不发送 Embedding 请求、不新增记忆；保存零条目的完成回执供排查。库中已有 3 个 entry_count=0 的回执。没有新增会话消息时，采集器不会生成新提取批次。
